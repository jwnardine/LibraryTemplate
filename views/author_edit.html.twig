<!DOCTYPE html>
<html>
  <head>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css'>
    <link rel='stylesheet' href='/styles.css'>
    <meta charset="utf-8">
    <title>Author Edit</title>
  </head>
  <body>
      <h1>Update {{ author.getFirstName }} {{ author.getLastName }}'s name:</h1>

      <form action="/authors_update/{{ author.getId }}" method="post">
        <input name="_method" type="hidden" value ="patch">

        <label for="first_name">Update First Name</label>
        <input id="first_name" name="first_name" type="text" value="{{ author.getFirstName }}">

        <br>

        <label for="last_name">Update Last Name</label>
        <input id="last_name" name="last_name" type="text" value="{{ author.getLastName }}">

        <button type="submit">Update</button>
      </form>

      <form action="/authors/{{ author.getId }}" method="post">
        <input name="_method" type="hidden" value="delete">

        <button type="submit">Delete this author!</button>
      </form>
  </body>
</html>
